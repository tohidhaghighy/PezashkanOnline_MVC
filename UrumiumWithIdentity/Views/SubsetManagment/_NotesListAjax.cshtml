@model List<UrumiumMVC.DomainClasses.Entities.Doctor.DoctorSubsetPassage>

@{
    foreach (var item in Model)
    {
        <tr>
            <td class="text-center" style="padding:10px;"> @item.Text</td>
           
            <td class="text-center" style="padding:10px;"><p data-placement="top" data-toggle="tooltip" title="حذف کن"><a class="btn btn-danger btn-xs" onclick="deleteitem(@item.Id)"><i class="ion ion-ios-trash"></i></a></p></td>
        </tr>
    }
}
